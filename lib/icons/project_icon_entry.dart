/// A stable business key mapped to one checked-in IconPark SVG asset.
class ProjectIconEntry {
  const ProjectIconEntry({required this.key, required this.assetPath});

  final String key;
  final String assetPath;
}
